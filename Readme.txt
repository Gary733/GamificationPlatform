1.der "platform" Ordner muss auf den Webserver. (Bei xampp in den htdocs ordner)
2.die config.php muss editiert werden und die datenbank logindaten müssen eingetragen werden.
3.Die Datenbank muss erstellt werden und die platform.sql in die datenbank importiert werden.

(Funktioniert gut mit xampp version 1.7.3)